# torch and torchvision are installed separately in Dockerfile with CUDA support
# Do not pin versions here to avoid conflicts with CUDA-specific wheels
numpy==1.26.1
Pillow
huggingface_hub
einops
safetensors
